The library is basically as same as "Fully Convolutional Networks for Semantic Segmentation". 
Just add an image_seg_data_layer interface which enable us to use an image as the lable (for segmentation).
Please refer to fcn_example for the usage.